import { HectogramToKilogramPipe } from './hectogram-to-kilogram.pipe';

describe('HectogramToKilogramPipe', () => {
  it('create an instance', () => {
    const pipe = new HectogramToKilogramPipe();
    expect(pipe).toBeTruthy();
  });
});
