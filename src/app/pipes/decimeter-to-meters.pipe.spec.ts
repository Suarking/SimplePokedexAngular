import { DecimeterToMetersPipe } from './decimeter-to-meters.pipe';

describe('DecimeterToMetersPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimeterToMetersPipe();
    expect(pipe).toBeTruthy();
  });
});
